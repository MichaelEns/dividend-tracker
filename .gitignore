__pycache__/
*.pyc
.venv/
.env
node_modules/
.wrangler/
docs/data.json.tmp

# Local secrets for `wrangler dev`. Real credentials live here and in
# `wrangler secret put` on the deployed worker - never in tracked files.
.dev.vars
.dev.vars.*
