{
  "name": "divtracker-plaid",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Cloudflare Worker that proxies Plaid Link + investment holdings for the divtracker page.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
